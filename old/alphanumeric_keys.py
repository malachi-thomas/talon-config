# from talon import Module

# mod = Module()

# @mod.capture(rule="{self.alphabet} | {self.symbol_keys} | {self.extra_symbol_keys} | {self.number_key}")
# def any_alphanumeric_key(m) -> str:
#   return str(m)
